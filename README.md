# cvd-risk-prediction-mlops

<!-- Build status badge: вставьте сюда badge GitHub Actions после публикации репозитория -->

Репозиторий магистерского ML/MLOps-проекта по теме прогнозирования риска сердечно-сосудистых осложнений на основе ретроспективных данных пациентов. Проект оформлен как инженерно аккуратная база для работы с данными, обучения моделей, воспроизводимого запуска и автоматических проверок качества кода.

## Предметная область

Проект относится к области медицинских интеллектуальных систем и предиктивной аналитики. Основная задача состоит в анализе обезличенных ретроспективных клинических данных, выделении значимых факторов риска и построении моделей, помогающих оценивать вероятность сердечно-сосудистых осложнений.

## Цель проекта

Цель проекта — разработать воспроизводимый прототип интеллектуальной системы, которая:

- работает с ретроспективными медицинскими данными;
- поддерживает подготовку и версионирование датасетов;
- позволяет обучать и проверять ML-модели;
- дает основу для дальнейшего развития в сторону инференса, API и клинического использования.

## Структура репозитория

```text
cvd-risk-prediction-mlops/
├── .dvc/                     # конфигурация DVC
├── .github/workflows/        # CI workflow для GitHub Actions
├── data/
│   ├── raw/                  # сырые данные, отслеживаемые через DVC
│   ├── interim/              # промежуточные данные после очистки
│   └── processed/            # подготовленные данные для моделирования
├── models/                   # локальные артефакты моделей
├── notebooks/                # исследовательские ноутбуки
├── src/
│   ├── data/                 # загрузка и подготовка данных
│   ├── features/             # генерация признаков
│   ├── inference/            # логика инференса
│   ├── models/               # обучение и оценка моделей
│   ├── utils/                # вспомогательные модули
│   └── main.py               # минимальная точка входа проекта
├── tests/                    # тесты проекта
├── .dockerignore             # исключения для Docker-сборки
├── .dvcignore                # исключения для DVC
├── .gitignore                # исключения Git
├── Dockerfile                # контейнеризация проекта
├── pyproject.toml            # конфигурация Python-инструментов
├── README.md                 # документация проекта
└── requirements.txt          # зависимости проекта
```

Дополнительно в локальной среде используется каталог `local-notes/`, который не попадает в Git и нужен для рабочих заметок и экспорта чатов.

## Стек технологий

- `Python 3.11+` — основная среда разработки
- `DVC` — версионирование датасетов и артефактов
- `Docker` — воспроизводимый запуск проекта в контейнере
- `GitHub Actions` — CI-проверки при Pull Request
- `pytest` — тестирование
- `flake8` — базовая проверка стиля кода
- `black` — контроль форматирования

## Git Flow

В проекте используется упрощенный Git Flow:

- `main` — стабильная ветка
- `develop` — основная ветка разработки
- `feature/*` — ветки под отдельные задачи и этапы лабораторной работы

Рекомендуемый сценарий работы:

1. Новая задача создается из `develop`.
2. Работа ведется в ветке вида `feature/<task-name>`.
3. После завершения изменений создается Pull Request в `develop`.
4. Перед слиянием изменения проходят CI-проверки.
5. Стабильные результаты позже переносятся в `main`.

## Быстрый старт

### Клонирование репозитория

```bash
git clone <repo-url>
cd cvd-risk-prediction-mlops
```

### Установка зависимостей

```bash
python -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

Для Windows PowerShell:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt
```

## Работа с DVC

`DVC` нужен для того, чтобы не хранить сырые датасеты напрямую в Git, но при этом сохранять воспроизводимость экспериментов.

### Инициализация DVC

```bash
git init
pip install dvc
dvc init
```

### Добавление датасета

Пример для файла `data/raw/dataset.csv`:

```bash
dvc add data/raw/dataset.csv
git add data/raw/dataset.csv.dvc .gitignore .dvc/config .dvcignore
git commit -m "data: track dataset with dvc"
```

Что важно:

- реальные файлы данных не коммитятся в Git;
- в Git попадают `.dvc`-файлы и конфигурация DVC;
- сами данные хранятся локально или в remote storage.

### Настройка remote storage

Пример для `S3`:

```bash
dvc remote add -d storage s3://your-bucket-name/cvd-risk-prediction
dvc remote modify storage region your-region
```

Пример для `Google Drive`:

```bash
dvc remote add -d storage gdrive://your-google-drive-folder-id
```

Замените шаблонные значения на свои. Секреты и токены не должны храниться в репозитории.

### Получение данных на новой машине

```bash
git clone <repo-url>
cd cvd-risk-prediction-mlops
pip install -r requirements.txt
dvc pull
```

## Docker

`Docker` нужен для воспроизводимого запуска проекта в одинаковой среде на локальной машине и в CI.

### Сборка Docker-образа

```bash
docker build -t cvd-risk-prediction-mlops .
```

### Запуск контейнера

```bash
docker run --rm cvd-risk-prediction-mlops
```

Если нужно подключить локальную папку с данными:

```bash
docker run --rm -v ${PWD}/data:/app/data cvd-risk-prediction-mlops
```

По умолчанию контейнер запускает минимальную точку входа `src/main.py`.

## CI/CD

Для проекта настроен workflow GitHub Actions в `.github/workflows/ci.yml`.

При создании `Pull Request` в ветку `develop` автоматически выполняются:

- установка `Python 3.11`;
- установка зависимостей проекта;
- запуск `flake8`;
- запуск `black --check`;
- запуск `pytest`.

Это позволяет автоматически проверять базовое качество кода до слияния изменений в `develop`.

## Локальные проверки

Перед отправкой изменений полезно запускать:

```bash
flake8 src tests
black --check src tests
pytest
```

## Примечание

Проект является учебной инженерной основой для дальнейшего развития. Реальные медицинские данные должны использоваться только в обезличенном виде, а их хранение и передача должны соответствовать требованиям конфиденциальности.
